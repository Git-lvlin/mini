
<view class="list">
  <view class="title-box">
    <span class="after">—</span>
    <span class="title">优选推荐</span>
    <span class="after">—</span>
  </view>
  
  <view class="tips-box">
    <text class="tips-after">!</text>
    <text class="tips-text">优选商品当前店铺没有售卖，点击提醒店主去采购</text>
  </view>

  <block wx:for="{{listData}}" wx:key="id">
    <view class="list-item">
      <van-image
        class="img"
        width="200rpx"
        height="200rpx"
        src="{{item.goodsImageUrl}}"
      />
      <view class="right-box">
        <view class="name">{{item.goodsName}}</view>
        <view class="price-box">
          <text class="price1"><text class="span">¥</text>{{item.goodsSalePrice/100}}</text>
          <text class="price2">{{item.goodsSalePrice/100}}</text>
        </view>
        <view class="btn {{btnType==1?'ed':''}}">{{btnType==1?'已提醒':'提醒店主采购'}}</view>
      </view>
    </view>
  </block>
  
</view>
