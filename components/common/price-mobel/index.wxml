<wxs module="tools" src="../../../wxs/common.wxs" />

<view class="price_mobel" style="color: {{defaultColor}};font-size: {{!!minSize ? minSize : 28}}rpx">
  <view class="title" wx:if="{{!!title}}">{{title}}</view>
  <view class="price" style="color: {{color}};{{bold ? 'font-weight: 600' : ''}}">
    <text class="unit">¥</text>
    <text class="max_price" style="{{!!maxSize ? maxSize : 40}}">{{tools.getPriceSplit(money)}}</text>
    <text class="min_price">{{tools.getPriceSplit(money, 1)}}</text>
  </view>
</view>